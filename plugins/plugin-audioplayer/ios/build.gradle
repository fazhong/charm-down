dependencies {
    compile project(':core/ios')
    compile project(':plugin-audioplayer')
}

ext.description = 'API to access audio player features for the iOS platform'

def service = "AudioPlayer"

task xcodebuild {
    doLast {
        xcodebuildIOS("$project.buildDir","$project.projectDir", "$service")
    }
}

jar {
    dependsOn xcodebuild
    from("$project.buildDir/native/lib${service}.a")
    {
        into "native"
    }
}
